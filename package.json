{
  "scripts": {
    "test": "jest --runInBand --updateSnapshot --detectOpenHandles --forceExit --coverage"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "@types/jest": "^29.5.14",
    "firebase": "^11.6.0",
    "jest": "^29.7.0"
  }
}
